const builder = require("electron-builder");

builder.build({
  config: {
    appId: "local.happa8.textdisplay",
    productName: "textdisplay",
    directories: {
      output: "dist"
    },
    win: {
      target: "portable"
    }
  }
});
